<% credit_type = CreditType.find(@credit.credit_type_id) %>
<h1 class="well"> Credit information by <%= credit_type.kind %> <h1>
<h4>Percent</h1>
<h5><%= credit_type.percent %> %</h3>
<hr>
<h4>Duration</h1>
<h5><%= credit_type.credit_duration%> month</h3>
<hr>
<h4>Max sum</h1>
<h5><%= @credit.sum %> <%= Currency.find(credit_type.currency_id).kind_name %></h3>
<hr>
<h4>Was Started at</h1>
<h5><%= @credit.created_at %></h3>
<hr>
<h4> Month Passed </h4>
<h5> <%= (DateTime.now.hour*60 + DateTime.now.min) - ((@credit.created_at.hour+3) * 60 + @credit.created_at.min) %> </h5>